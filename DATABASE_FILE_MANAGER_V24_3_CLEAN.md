# Database File Manager V24.3 — Clean

## Логика экрана

При открытии БД:

```text
[БД сервера] [БД моя] [БД мастеров]
```

`БД мастеров` видит только админ.

## Редактор БД

Блок `Редактор БД` по умолчанию свернут.

Если выбрана `БД моя`, внутри редактора есть импорт из `БД сервера`:

- папки;
- подпапки;
- позиции;
- выделить всё;
- добавить в Мою БД.

## Основные разделы

```text
[Работа] [Материал]
```

Внутри:

```text
Папка -> подпапка -> карточка позиции
```

## Импорт JSON

Поддерживается формат пользователя:

```json
{
  "source": "my",
  "matDB": [],
  "workDB": [
    { "id": "...", "n": "Название", "p": 30, "u": "час", "c": "Категория", "sc": "Подкатегория", "g": "Группа" }
  ]
}
```

## Склад

Склад пока отдельным блоком-фундаментом. Следующим шагом можно делать остатки/приход/списание.
