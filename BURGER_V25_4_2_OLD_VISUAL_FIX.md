# Burger V25.4.2 — Old Visual Fix

Исправлена ошибка установки V25.4.1:
скрипт делал `cd` в проект и потом искал файлы архива в проекте.

Теперь установщик берёт файлы из своей папки архива через `SCRIPT_DIR`.

## Исправлено

- старый визуал бургер-панели;
- один профиль;
- один бейдж справа;
- БД открывается через монолит;
- конфликтующие shell-патчи отключаются.
