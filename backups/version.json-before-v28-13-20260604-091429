{"version":"V28.6-safety-fix","updatedAt":"2026-06-03T09:00:00.000Z","note":"V28.6 Safety Fix: (2) firestore.rules profile create no longer allows self-assigning role=admin/status=approved (forced master/pending); (3) V15_2 admin-read match blocks moved INSIDE match /databases/{database}/documents (rules now deploy); (4) subscription-api sends periodDays (was days) for grant + payment request, function also accepts days fallback; (5) grantSubscription honors explicit trial flag so pro_ai trial -> status trial (was basic-only); (6) admin panel writes subscriptions and AI balance via Cloud Functions (grantSubscription/cancelSubscription/setAiAccessMode/setAiBalanceExact) instead of direct writes to protected collections. (1) db-params-v28.js present (window.EPDbParamsV28)."}
