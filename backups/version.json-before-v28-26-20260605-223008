{"version":"V28.25-gost-view","updatedAt":"2026-06-05T15:00:00.000Z","note":"V28.25 First pass of the GOST-style single-line view (like the reference photo): horizontal L1/L2/L3 + PEN busbar with colours, input chain on top, each group (RCD/Diff) dropping to a sub-busbar with line breakers, load symbols at the bottom (lamp/socket/boiler/stove/motor/generic) with line names, cable labels rotated along the drops, frame + title block. Added a ГОСТ/Дерево toggle in the manual builder preview (default ГОСТ); the old tree view remains. Fullscreen + pinch zoom work with both. Configurator scheme still uses the tree view for now; will switch after the look is refined."}
