{"version":"V28.17-scheme-ux","updatedAt":"2026-06-05T08:00:00.000Z","note":"V28.17 Manual single-line UX: (1) per-line current characteristic (curve B/C/D) + ampere select; (2) brand selector at the top of the builder with ability to add custom brands (shared with configurator dropdowns), passed to compute; (4) zoom in/out on the preview; (5) fixed scheme engine — replaced the white text background rectangle (which overlapped neighbouring lines) with a white text halo so labels are readable without covering wires. Cable auto-suggestion now keyed by ampere so it works for any curve. Image-2 style professional layout is a separate next step."}
